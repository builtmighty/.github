{
	"blocks": [
		{
			"type": "divider"
		},
		{
			"type": "header",
			"text": {
				"type": "plain_text",
				"text": "🛑 Deployment Failed",
				"emoji": true
			}
		},
		{
			"type": "section",
			"text": {
				"type": "mrkdwn",
				"text": "<!here> There are *uncommitted changes* on the production server."
			}
		},
		{
			"type": "image",
			"image_url": "https://i.giphy.com/media/P8X4lemg5Iphe/giphy.webp",
			"alt_text": "inspiration"
		},
		{
			"type": "section",
			"text": {
				"type": "mrkdwn",
				"text": "🚩 *Result*: `${{ job.status }}`\n🚀 *Triggered By*: `${{ github.event.pull_request.html_url || github.event.head_commit.url }}`\n🧑‍💻 *User*: `${{ github.triggering_actor }}`\n🔀 *Un-commmitted Branch*: `<${{ env.GIT_REPO_URL }}/tree/${{env.UNCOMMITTED_BRANCH}}|${{env.UNCOMMITTED_BRANCH}}>`"
			}
		}
	]
}
